# Baked into the Vercel build (Vite reads this automatically on `vite build`).
# NOTE: a VITE_API_URL set in the Vercel dashboard OVERRIDES this file — delete it there if stale.
VITE_API_URL=https://unibridge-bv20.onrender.com
VITE_SOCKET_URL=wss://unibridge-bv20.onrender.com
VITE_APP_NAME=UniPortal
